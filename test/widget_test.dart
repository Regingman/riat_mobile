import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Test User toJson', () {
    expect(true, true);
  });
  test('Test taskStatistic taskProcent', () {
    expect(true, true);
  });
  test('Test taskStatistic toDate', () {
    expect(true, true);
  });
  test('Test taskStatistic toJson', () {
    expect(true, true);
  });
  test('Test Message timeDifference', () {
    expect(true, true);
  });

  


  //
  test('Test Task toJson', () {
    expect(true, true);
  });
  test('Test Department toJson', () {
    expect(true, true);
  });
  test('Test EmployeeTask toJson', () {
    expect(true, true);
  });
  test('Test ChatModel toJson', () {
    expect(true, true);
  });
  test('Test Statistic toJson', () {
    expect(true, true);
  });
  
  //
  test('Test Task createTask', () {
    expect(true, true);
  });
  test('Test StoryModel icons', () {
    expect(true, true);
  });
  }
